// Shared between AuthGate (schedules the unlock click at login) and the
// splash screen in App.jsx (fades in over this same duration).
export const SPLASH_MIN_MS = 2000;
export const SPLASH_CLICK_MS = 1000;
